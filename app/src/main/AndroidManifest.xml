<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gerenciadordelivros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_my_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_my_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".views.ListarLivroAutorActivity"
            android:label="@string/title_activity_listar_livro_autor"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".views.LivroAutorActivity"
            android:label="@string/title_activity_livro_autor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".views.ActivityMenu" />
        <activity android:name=".views.EditarLivroActivity" />
        <activity android:name=".views.EditarAutorActivity" />
        <activity android:name=".views.EditarEditoraActivity" />
        <activity android:name=".views.EditarUsuarioActivity" />
        <activity android:name=".views.LivroActivity" />
        <activity android:name=".views.AutorActivity" />
        <activity android:name=".views.EditoraActivity" />
        <activity android:name=".views.UsuarioActivity" />
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>